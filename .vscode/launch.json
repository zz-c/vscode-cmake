{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "ctest",
            "type":"cppdbg",
            "request": "launch",
            "program": "${workspaceRoot}/build/swap/swap.exe",
            "cwd":"${workspaceRoot}",
            "args": [],
            "preLaunchTask": "BuildTask",
        }
    ]
}
